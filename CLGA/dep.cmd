REM gensim uses scipy functionality that was removed in scipy versions >= 1.13 so we need to install an older version
pip install scipy==1.12
pip install torch
pip install torch_geometric
pip install ogb
pip install numba
pip install gensim
pip install pyyaml
pip install nni